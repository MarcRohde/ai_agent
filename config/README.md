# Config Directory

This directory contains configuration files that control skill behavior and defaults.

## Files

| File | Purpose |
|------|---------|
| `skill_config.json` | Default settings for skills (output format, verbosity, etc.) |
